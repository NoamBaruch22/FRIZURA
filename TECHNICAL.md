# תיעוד ארכיטקטוני (TECHNICAL.md)

מסמך זה נועד לפרט את הארכיטקטורה, מנגנוני הליבה ואבטחת המידע העומדים בבסיס פרויקט FRIZURA.

## 🏗️ ארכיטקטורה מודולרית

הפרויקט בנוי בארכיטקטורת שכבות (Layered Architecture) המפרידה לחלוטין בין התצוגה (UI), הלוגיקה העסקית (Core), ובסיס הנתונים (Database):

1. **שכבת מסד נתונים (`database/db_manager.py`):**
   - אחראית על ניהול החיבור ל-SQLite3.
   - מטפלת באתחול הסכמה, הגדרות החיבור ושליפת נתונים בסיסית.
2. **שכבת הלוגיקה העסקית (`core/`):**
   - מופרדת לשני מנהלים:
     - `CRMManager`: מנהל את ישויות הלקוחות והלידים, המרת ליד ללקוח ושחזור/מחיקה.
     - `OperationsManager`: מנהל את התורים, החשבוניות ובדיקות חפיפת זמנים.
   - שכבה זו מכילה את הכללים העסקיים, וידוא התקינות ועיבוד הנתונים לפני הצגתם.
3. **שכבת תצוגה וממשק משתמש (`ui/`):**
   - `gui.py`: ממשק משתמש חלונאי מתקדם (CustomTkinter) המתקשר אך ורק עם שכבת הלוגיקה (Core) ולעולם לא ישירות עם טבלאות ה-DB.

הפרדה זו מאפשרת תחזוקה קלה, החלפת ממשק משתמש (למשל מ-CLI ל-GUI או Web) מבלי לשנות את ליבת המערכת.

## 🛡️ מנגנון "Soft Delete" וטריגרים במסד הנתונים

המערכת משתמשת בגישת **Soft Delete** במקום למחוק רשומות כליל. 
- לכל ישות קריטית מוגדר דגל בוליאני `is_deleted`. 
- כאשר לקוח "נמחק" במערכת, ערך הדגל משתנה ל-`1`. השאילתות הרגילות מסננות רשומות אלו (`WHERE is_deleted=0`), בעוד שמסך ה"ארכיון" שולף אותן לצרכי צפייה או שחזור.
- **Trigger Cascade ב-SQLite:** כדי למנוע חוסר עקביות, הוטמע טריגר ברמת מסד הנתונים. כאשר לקוח מועבר למצב מחוק (`is_deleted = 1`), הטריגר רץ באופן אוטומטי מאחורי הקלעים ומעדכן את כל התורים של אותו לקוח כך שיימחקו רעיונית גם כן, מבלי להצריך קוד ארוך ומורכב בשכבת הפייתון.

## 🔒 אבטחת מידע - מניעת SQL Injection

כלל השאילתות במערכת מבוססות על **שאילתות פרמטריות (Parameterized Queries)** תוך שימוש בסימן השאלה `?` הנתמך ב-`sqlite3`.
- המערכת לעולם לא משרשרת מחרוזות (String Concatenation) בעת הזרקת קלט ממשתמש לתוך משפטי ה-SQL.
- גישה זו מבטיחה שכל קלט (גם זדוני) מטופל כטקסט בלבד על ידי מנוע מסד הנתונים, מה שמגן על המערכת מפני ניסיונות SQL Injection.

## ⚙️ הגדרות וביצועי מסד הנתונים (SQLite)

עם כל פתיחת חיבור למסד הנתונים, המערכת מריצה שתי פקודות PRAGMA חשובות:
1. **אכיפת מפתחות זרים (`PRAGMA foreign_keys = ON;`):**
   מבטיחה שלמות נתונים (Referential Integrity). למשל, לא ניתן יהיה ליצור תור עם מזהה לקוח שאינו קיים בטבלת הלקוחות.
2. **מצב ביצועים מקסימלי (`PRAGMA journal_mode = WAL;`):**
   הפעלת מצב Write-Ahead Logging המשפר דרמטית את ביצועי הקריאה והכתיבה, ומאפשר גישה מקבילית יעילה יותר למסד הנתונים לעומת מנגנון העסקאות הסטנדרטי.
